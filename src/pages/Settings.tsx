import { Card, Typography } from 'antd';

export default function Settings() {
  return (
    <div>
      <Typography.Title level={3}>Ayarlar</Typography.Title>
      <Card>
        <Typography.Text>Sistem ayarları sayfası — yakında içerik eklenecek.</Typography.Text>
      </Card>
    </div>
  );
}
